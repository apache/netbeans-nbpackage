#!/usr/bin/env bash
if [ -d "_install_path_/jdk" ] ; then
  if [ -z "$JAVA_HOME" ] ; then
    export JAVA_HOME="_install_path_/jdk/"
  fi
  exec "_exec_path_" --jdkhome "_install_path_/jdk" "$@"
else
  exec "_exec_path_" "$@"
fi
